import { MsgForDirective } from './msg-for.directive';

describe('MsgForDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgForDirective();
    expect(directive).toBeTruthy();
  });
});
